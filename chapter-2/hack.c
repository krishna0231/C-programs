#include<stdio.h>
#include<coino.h>
int main(void)
{
    int a,b,c;
    //clrscr();
    a=10;      // 10 is decimal
    b=010;     // 010 is octal
    c=0*Ff;     //0xFf is a hexadecimal
    printf("Decimal is %d",a);
    printf("\nOctaldecimal integer equivalent in decimal is %d", b);
    printf("\nHexadecimal integer is equivalent to in decimal %d",c);
    getch();
    return 0;
    

}