//sizeof operetor
#include<stdio.h>
#include<conio.h>
int main()
{
    int i; float f; char c;
    printf("fint,float and char size are:%d%d%d",sizeof(i), sizeof(i),sizeof(f),sizeof(c) );
    return 0;
}
