#include<stdio.h>
#include<conio.h>
int main ()
{
    float n=10.5;
    char gender='M';
    printf("Memory allocated by  n:%d", sizeof(n));
    printf("\n memory allocated by gender :%d",sizeof(gender));
    getch();
    return 0;
}